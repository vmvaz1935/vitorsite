/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Allow loading external images if needed in the future.
    domains: [],
  },
};

module.exports = nextConfig;